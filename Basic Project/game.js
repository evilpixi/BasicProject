var config ={
    width : 480,
    height : 360,
    backgroundColor: 0x000000,
    scene: [Scene1, PlayScene],
	render: {
    pixelArt: true
	},
}
      
var game = new Phaser.Game(config);